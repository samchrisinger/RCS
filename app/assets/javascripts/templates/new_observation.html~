<h3>
  <em>New</em> observation by {{current_user.first_name[0]}}. {{current.last_name}} on  
  <span class="input-group col-md-2">
    <input type="text" class="form-control observationDP" is-open="dp.opened" datepicker-options="dp" datepicker-popup="MMMM dd yyyy" ng-model="observation.timestamp" min-date="minDate" max-date="maxDate" close-text="Close" />
    <span class="input-group-btn">
      <button type="button" class="btn btn-default" ng-click="dp.open($event)"><i class="glyphicon glyphicon-calendar"></i></button>
    </span>
  </span>
</h3>
<div class="row">
<table class="table col-md-4" id="observationMetricsTable">
  <thead>
    <tr>
      <th>
	Type
      </th>
      <th>
	Value
      </th>
    </tr>
  </thead>
  <tbody>
    <tr ng-repeat="metric in observation.metrics">
      <td>	
	{{metric.type.name}}
      </td>
      <td>
	{{metric.value}}
      </td>
    </tr>
  </tbody>
</table>
<img class="observationImage col-md-12" src="{{observation.photo_url}}"></img>
</div>

